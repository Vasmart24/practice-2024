import "farmsButton.css";
import Button from "../Button";

export default function farmButton({ onClick, disabled }) {

    return (
        <Button onClick={onClick} className={} disabled={disabled}>
            <div>Ур0</div>
            <div>Шт0</div>
        </Button>
    );
}